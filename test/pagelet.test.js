describe('pagelet', function () {
  'use strict';

  var collection = require('../pagelet')
    , assume = require('assume');

  describe('.configure', function () {
    it('registers a local event listener to destroy if data.remove is true');
    it('unauthorized pagelets self-destruct after everything is done');
  });
});